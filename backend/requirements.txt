# --- API layer ---
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-socketio==5.13.0

# --- Agent runtime ---
mcp==1.16.0
google-genai==1.39.1
httpx==0.28.1

# --- State store ---
# pymongo 4.13+ ships a native async client, so motor (EOL) is not needed.
pymongo==4.15.1

# --- Support ---
pydantic==2.12.0
python-dotenv==1.1.1

# --- Dev/test ---
pytest==8.4.2
pytest-asyncio==1.2.0
# aiohttp is only used by scripts/smoke_api_live.py (the Socket.io client
# library needs it for its own HTTP transport) -- not required to run the app
aiohttp==3.14.3
